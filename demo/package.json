{
  "name": "threejs-starter-vite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cd .. && npm run build && cd demo && npm i --save ../ && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "@fluid-lib/utils": "file:..",
    "lil-gui": "^0.19.2",
    "three": "^0.172.0"
  }
}
