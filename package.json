{
  "name": "@evenstart/fluid-root",
  "private": true,
  "type": "module",
  "workspaces": [
    "lib",
    "docs"
  ],
  "scripts": {
    "build:lib": "yarn workspace @evenstar/fluid build",
    "build:docs": "yarn workspace docs build",
    "build": "yarn build:lib && yarn build:docs"
  }
}
